import Button from "../../../components/Button";import GlassCard from "../../../components/GlassCard";import Input from "../../../components/Input";import PageHeader from "../../../components/PageHeader";
export default function Support(){return <><PageHeader eyebrow="Support center" title="Get unstuck quickly">Browse help topics or raise a priority ticket with the admissions operations team.</PageHeader><GlassCard className="max-w-4xl"><Input placeholder="Search help articles: refunds, bookings, SOP upload..."/><div className="mt-6 grid gap-4 md:grid-cols-3">{["Bookings","Billing","Applications"].map(x=><div key={x} className="rounded-2xl bg-white/60 p-4 font-bold">{x}</div>)}</div><Button className="mt-6">Create support ticket</Button></GlassCard></>}
