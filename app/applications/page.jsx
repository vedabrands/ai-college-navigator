import GlassCard from "../../components/GlassCard";import PageHeader from "../../components/PageHeader";import { applications } from "../../data/mock";
export default function Applications(){return <><PageHeader eyebrow="Applications tracker" title="Every deadline under control">Visual pipeline for exams, essays, recommendation letters, interviews, deposits, and scholarship milestones.</PageHeader><div className="space-y-5">{applications.map(([name,status,desc,progress])=><GlassCard key={name}><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><p className="text-xl font-black">{name}</p><p className="text-sm text-slate-500">{desc}</p></div><span className="rounded-full bg-cyan-100 px-4 py-2 text-sm font-black text-cyan-700">{status}</span></div><div className="mt-5 h-3 rounded-full bg-white/70"><div className="h-3 rounded-full bg-gradient-to-r from-blue-600 to-cyan-400" style={{width:`${progress}%`}}/></div></GlassCard>)}</div></>}
